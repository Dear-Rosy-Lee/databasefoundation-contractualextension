<UserControl x:Class="YuLinTu.Library.Controls.InvestigationOwnershipPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YuLinTu.Library.Controls"
             xmlns:metro="clr-namespace:YuLinTu.Windows.Wpf.Metro;assembly=YuLinTu.Windows.Wpf.Metro"
             xmlns:cmps="clr-namespace:YuLinTu.Windows.Wpf.Metro.Components;assembly=YuLinTu.Windows.Wpf.Metro">

    <ScrollViewer VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
        <Grid>
            <!--<cmps:LoadingIcon Width="16"
                          Height="16"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"
                          Margin="20,10,5,0"
                           />-->
            <TextBlock Name="txtNotFound" VerticalAlignment="Center"  HorizontalAlignment="Center"
                       Text="没有需要显示的项"
                       Foreground="{DynamicResource Metro_Window_Style_Foreground_Lighter}"></TextBlock>
            <Grid Visibility="{Binding ContentVisiblity}" x:Name="grid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <local:InvestigationSharePerson x:Name="panelSharePerson" Margin="0"  Grid.Row="0"/>
                
                <local:InvestigationLandPanel x:Name="panelLand"  Margin="0" Grid.Row="1"/>
                
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>

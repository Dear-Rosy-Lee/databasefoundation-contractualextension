<cmps:TabMessageBox
    x:Class="YuLinTu.Library.Controls.ConfigPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cmps="clr-namespace:YuLinTu.Windows.Wpf.Metro.Components;assembly=YuLinTu.Windows.Wpf.Metro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:YuLinTu.Library.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:metro="clr-namespace:YuLinTu.Windows.Wpf.Metro;assembly=YuLinTu.Windows.Wpf.Metro"
    xmlns:wpftoolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
    MaxWidth="1150"
    MaxHeight="640"
    CloseButtonVisibility="Collapsed"
    Icon="pack://application:,,,/YuLinTu.Resources;component/Images/16/property.png"
    mc:Ignorable="d">
    <cmps:TabMessageBox.Resources>

        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border Name="Border" Background="Transparent">
                            <ContentPresenter
                                x:Name="ContentSite"
                                Margin="12,10,12,10"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                ContentSource="Header"
                                RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource Metro_Button_Style_Background_Hover}" />
                            </Trigger>

                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource Metro_Button_Style_Background_Pressed}" />
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="TabControlStyle" TargetType="{x:Type TabControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <Grid
                            ClipToBounds="true"
                            KeyboardNavigation.TabNavigation="Local"
                            SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="100*" />
                            </Grid.ColumnDefinitions>
                            <Border
                                x:Name="ContentPanel"
                                Grid.Row="0"
                                Grid.RowSpan="1"
                                Grid.Column="1"
                                Grid.ColumnSpan="1"
                                Width="Auto"
                                Margin="0"
                                BorderThickness="1,0,0,0"
                                KeyboardNavigation.DirectionalNavigation="Contained"
                                KeyboardNavigation.TabIndex="2"
                                KeyboardNavigation.TabNavigation="Local">
                                <ContentPresenter
                                    x:Name="PART_SelectedContentHost"
                                    Width="Auto"
                                    Margin="0"
                                    ContentSource="SelectedContent"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Border>
                            
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--<Style x:Key="ChkStyle" TargetType="CheckBox">
            <Setter Property="VerticalAlignment"  Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <EventSetter Event="Checked" Handler="childCBClick"/>
            <EventSetter Event="Unchecked" Handler="childCBClick"/>
        </Style>-->

        <Style x:Key="TxtStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin" Value="5,0,15,0" />
        </Style>
    </cmps:TabMessageBox.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TabControl
            x:Name="tcInfo"
            Width="970"
            Height="505"
            BorderThickness="0"
            Style="{StaticResource TabControlStyle}">

            <TabItem Header="数据检查配置">
                <ScrollViewer
                    BorderThickness="0"
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <cmps:MetroExpander
                            Margin="0,0,0,-1"
                            BorderThickness="0"
                            HeaderBorderThickness="0,1,0,1"
                            HeaderPadding="10,4,10,4"
                            IsExpanded="True">
                            <cmps:MetroExpander.Header>

                                <DockPanel VerticalAlignment="Center" LastChildFill="True">
                                    <StackPanel
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Orientation="Horizontal">
                                        <Image
                                            Grid.Column="1"
                                            Margin="0,0,5,0"
                                            VerticalAlignment="Center"
                                            DockPanel.Dock="Left"
                                            RenderOptions.BitmapScalingMode="NearestNeighbor"
                                            SnapsToDevicePixels="True"
                                            Stretch="None" />
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontSize="14"
                                            FontWeight="Bold"
                                            Text="数据检查配置" />
                                    </StackPanel>


                                </DockPanel>
                            </cmps:MetroExpander.Header>
                            <StackPanel Margin="47,5,0,5" HorizontalAlignment="Left" />
                        </cmps:MetroExpander>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Height="40" Header="身份证忽略配置">
                <ScrollViewer
                    BorderThickness="0"
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalScrollBarVisibility="Auto" />
            </TabItem>
        </TabControl>

        <TextBlock
            x:Name="lbError"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            TextWrapping="Wrap" />
        <Border
            Grid.Row="1"
            Margin="0,0,0,5"
            BorderBrush="{DynamicResource Metro_Window_Style_BorderBrush_Default}"
            BorderThickness="0,1,0,0"
            SnapsToDevicePixels="True" />
        <DockPanel
            Grid.Row="2"
            Margin="10,0,10,5"
            LastChildFill="True">
            <metro:MetroButton
                x:Name="canc"
                Height="30"
                MinWidth="75"
                Margin="10,0,5,0"
                Click="canc_Click"
                Command="{x:Static metro:MetroCommands.CloseTabMessageBoxCommand}"
                DockPanel.Dock="Right"
                Style="{StaticResource Metro_Button_Paneled_Style}">
                <StackPanel Orientation="Horizontal">
                    <Image
                        RenderOptions.BitmapScalingMode="NearestNeighbor"
                        SnapsToDevicePixels="True"
                        Source="pack://application:,,,/YuLinTu.Resources;component/Images/16/application--minus.png"
                        Stretch="None" />
                    <TextBlock
                        Margin="5,0,0,0"
                        VerticalAlignment="Center"
                        Text="取消" />
                </StackPanel>
            </metro:MetroButton>
            <metro:MetroButton
                x:Name="btnSave"
                Height="30"
                MinWidth="75"
                Margin="10,0,0,0"
                Click="btnSave_Click"
                Command="{x:Static metro:MetroCommands.ConfirmTabMessageBoxCommand}"
                DockPanel.Dock="Right"
                Style="{StaticResource Metro_Button_Paneled_Style}">
                <StackPanel Orientation="Horizontal">
                    <Image
                        VerticalAlignment="Center"
                        RenderOptions.BitmapScalingMode="NearestNeighbor"
                        SnapsToDevicePixels="True"
                        Source="pack://application:,,,/YuLinTu.Resources;component/Images/16/tick.png"
                        Stretch="None" />
                    <TextBlock
                        Margin="5,0,0,0"
                        VerticalAlignment="Center"
                        Text="确定" />
                </StackPanel>
            </metro:MetroButton>

            <Label
                Name="lbInfo"
                VerticalAlignment="Center"
                Foreground="Red" />
        </DockPanel>
    </Grid>
</cmps:TabMessageBox>
